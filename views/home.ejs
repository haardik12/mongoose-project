<!-- <html>
    <head>
        <title>
            <%= title %>
        </title>
    </head>
    <body> -->
<!-- this is an example of partials -->
<!-- seperate file for header and footer and we include it with the desired extensions -->
<!-- <%- include('_header'); %> -->
<link rel="stylesheet" href="/css/home.css" />

<div id="home-container">

  <section id="feed-posts">
    <h4>POSTS</h4>
    <% if (locals.user) { %>
    <form action="/posts/create" id="new-post-here" method="POST">
      <textarea
        name="content"
        cols="30"
        rows="3"
        placeholder="type here..."
        required
      ></textarea>
      <input type="submit" value="Post" />
    </form>
    <% } %>
    <div id="post-container">
      <ul>
        <% for (post of posts) { %> <%- include('_posts') -%> <% } %>
      </ul>
    </div>
  </section>

  <section id="user-friends">
    <h4>friends</h4>
    <% for(u of all_users) { %>
    <p>
      <a href="/users/profile/<%= u.id %>"><%= u.name %></a>
    </p>
    <% } %>
  </section>
</div>
<!-- <%- include('_footer'); %>
    </body>
</html> -->

<!-- commenting the whole part as it is duplicating everything except the html tags and rendering it on the web page  -->
<!-- leaving only the variable part which changes accrodingly  -->
